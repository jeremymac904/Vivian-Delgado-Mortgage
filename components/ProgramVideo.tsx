import React from "react";

type ProgramVideoProps = {
  slug: string;
};

const ProgramVideo: React.FC<ProgramVideoProps> = ({ slug }) => {
  void slug;
  return null;
};

export default ProgramVideo;
